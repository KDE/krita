if(LCMS2_FOUND)
  message(STATUS "LCMS version 2 was found, and it will be used.")
  add_subdirectory(lcms2)
else()
  if(HAVE_LCMS AND WIN32)
      message(STATUS "LCMS version 2 was not found, lcms v1 will be used instead.")
      add_subdirectory(lcms)
  else(HAVE_LCMS AND WIN32)
      message(STATUS "LCMS version 1 is no longer supported on Linux, please install LCMS version 2.")
  endif(HAVE_LCMS AND WIN32)
endif()

