# CMake external projects to build krita's dependencies on Linux, Windows or MacOS

These projects define all Krita's external dependencies and make it possible to build them on Linux, Windows or MacOS. 
